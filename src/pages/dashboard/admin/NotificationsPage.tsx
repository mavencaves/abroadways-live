import { NotificationCenterPage } from "@/components/notifications/NotificationCenterPage";

export default function NotificationsPage() {
  return (
    <NotificationCenterPage
      eyebrow="Dashboard"
      title="Notifications"
      description="Track assigned leads, overdue follow-ups, payment events, appointment updates, and other operational alerts in one internal inbox."
    />
  );
}
